#include <iostream>
#include <vector>
#include <math.h>
#include <stdio.h>
#include <algorithm>
#include <set>
#include <regex>
#include <iomanip>
#include <map>
#include <cassert>
using namespace std;
using ll = long long;
using P = pair<int, int>;
#define rep(i, n) for (int i = 0; i < n; i++)
#define rep2(i, n) for (int i = 1; i <= n; i++)

int main()
{
    cout << 7 / 8 << endl;
    // int n;
    // cin >> n;
    // int ans = 0;
    // rep2(i, n)
    // {
    //     bool ok = true;
    //     // 10進数判定
    //     {
    //         int base = 10;
    //         int x = i;
    //         while (x > 0)
    //         {
    //             if (x % base == 7)
    //                 ok = false;
    //             x /= base;
    //         }
    //     }
    //     // 8進数判定
    //     {
    //         int base = 8;
    //         int x = i;
    //         while (x > 0)
    //         {
    //             if (x % base == 7)
    //                 ok = false;
    //             x /= base;
    //         }
    //     }
    //     if (ok)
    //         ans++;
    // }
    // cout << ans << endl;
    return 0;
}