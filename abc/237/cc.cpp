#include <iostream>
#include <vector>
#include <math.h>
#include <stdio.h>
#include <algorithm>
#include <set>
#include <regex>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < n; i++)
 
int main()
{
    // ループを回す
    // 回文判定
    // 回文でなければ文字をつけたす
    // もう回文として成立しなければNoを出力
    
    return 0;
}