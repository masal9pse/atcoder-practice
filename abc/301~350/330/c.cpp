#include <iostream>
#include <vector>
#include <math.h>
#include <stdio.h>
#include <algorithm>
#include <set>
#include <regex>
#include <iomanip>
#include <map>
#include <cassert>
using namespace std;
using ll = long long;
using P = pair<int, int>;
#define rep(i, n) for (int i = 0; i < n; i++)
#define rep2(i, n) for (int i = 1; i <= n; i++)
#define rep3(i, n) for (int i = 0; i <= n; i++)
const double PI = acos(-1);

int main()
{
    /*
     x^2 + y^2をcとする
     最小値を全探索すればDはわかっているから、cも出るはず

     下記学べる
     全探索の範囲を求める
     片方だけ全探索、もう片方は計算
     誤差
     ルートが腑の場合
     平方根の計算量はわからないらしい
     2分探索、尺取り方(分からん)でもよい
    */

    int k = 2e6; // 20**6
    return 0;
}