#include <iostream>
#include <vector>
#include <math.h>
#include <stdio.h>
#include <algorithm>
#include <set>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < n; i++)

int main()
{
    int counter = 9;

    while (true)
    {

        cout << "Counter: " << counter << endl;
        counter--;

        if (counter == 0)
        {
            break;
        }
    }

    return 0;
}